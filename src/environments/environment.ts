// Este archivo puede reemplazarse durante el build usando el arreglo `fileReplacements`.
// `ng build` reemplaza `environment.ts` por `environment.prod.ts`.
// La lista de reemplazos de archivos se encuentra en `angular.json`.

export const environment = {
  production: false
};
